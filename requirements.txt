# Core Dependencies — Chinese Drama Dubber v6.1
yt-dlp>=2024.0.0
groq>=0.11.0
edge-tts>=6.1.0
nest-asyncio>=1.6.0
httpx>=0.27.0

# Transcription — faster-whisper (local GPU, ~4-6x faster than Whisper)
faster-whisper>=1.0.0

# Speaker Diarization (optional — requires HF_TOKEN set for pyannote)
# pyannote.audio>=3.0.0

# Audio Processing
demucs>=4.0.0
torchaudio>=2.0.0
torch>=2.0.0

# Utilities
unidecode>=2.0.0
indic-transliteration>=2.3.0

# Validation & Testing
pydantic>=2.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
